@model DashboardViewModel
@{
    ViewBag.Title = "Dashboard";
}

<div class="container-fluid">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-lg-3 col-6">
            <!-- small box -->
            @SmallBox(Model.CategoryCount.ToString(), "Categories", Url.Action("Index", "Categories"), "bg-danger", "fas fa-table")
        </div>
        <div class="col-lg-3 col-6">
            @SmallBox(Model.PostCount.ToString(), "Posts", Url.Action("Index", "Posts"), "bg-info", "far fa-file")
        </div>
        <div class="col-lg-3 col-6">
            @SmallBox(Model.CommentCount.ToString(), "Comments", Url.Action("Index", "Comments"), "bg-success", "fas fa-comments")
        </div>
        <div class="col-lg-3 col-6">
            @SmallBox(Model.UserCount.ToString(), "Users", "#", "bg-warning", "fas fa-users")
        </div>
    </div>
    <!-- /.row -->
</div>

@helper SmallBox(string title, string description, string link, string boxClass, string iconClass)
{
    <!-- small box -->
    <div class="small-box @boxClass">
        <div class="inner">
            <h3>@title</h3>

            <p>@description</p>
        </div>
        <div class="icon">
            <i class="@iconClass"></i>
        </div>
        <a href="@link" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
    </div>
}

